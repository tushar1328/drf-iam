Installation
============

To install DRF-IAM, you can use pip:

.. code-block:: bash

    pip install drf-iam

Requirements
-----------

* Python >= 3.6
* Django >= 2.2
* Django REST Framework >= 3.11
